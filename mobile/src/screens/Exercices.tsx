import React from 'react';
import {
  StyleSheet,
  Text,
} from 'react-native';

function Exercices(): JSX.Element {
  return <Text>Exercices Screen</Text>;
}

const styles = StyleSheet.create({});

export default Exercices;
