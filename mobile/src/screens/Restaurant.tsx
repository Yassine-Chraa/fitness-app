import React from 'react';
import {StyleSheet, Text} from 'react-native';

const Restaurant = (): JSX.Element => {
  return <Text>Restaurant Screen</Text>;
};

const styles = StyleSheet.create({});

export default Restaurant;
