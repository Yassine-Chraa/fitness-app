import React from 'react';
import {
  StyleSheet,
  Text,
} from 'react-native';

function VipUserProfile(): JSX.Element {
  return <Text>Vip User Profile</Text>;
}

const styles = StyleSheet.create({});

export default VipUserProfile;
